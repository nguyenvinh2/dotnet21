version: 0.2

phases:

  build:
    commands:
      - dotnet restore dotnet21.csproj
      - dotnet build dotnet21.csproj
      - dotnet publish dotnet21.csproj -o ./staging/app
artifacts:
  files:
    - '**/*'
  base-directory: './'